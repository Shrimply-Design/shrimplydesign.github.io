import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://shrimply.design',
  // For GitHub Pages deployment with custom domain, we don't need a base path
  base: '/',
});
